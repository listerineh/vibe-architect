/**
 * Custom React Hooks
 * 
 * Centralized export for all custom hooks in the application.
 * These hooks provide reusable logic for common patterns.
 */

export { useLocalStorage } from './useLocalStorage';
export { useMediaQuery } from './useMediaQuery';
